`Textarea` — texto de múltiplas linhas (observações, descrição do evento).

```jsx
<Textarea rows={4} placeholder="Observações do pedido" />
```

Redimensionável na vertical. Mesmo tratamento de foco/erro do `Input`.
