`EmptyState` — estado vazio com ícone circular, título, texto e ação. Sempre ofereça o próximo passo.

```jsx
<EmptyState
  icon={<FileText size={26} />}
  title="Nenhum orçamento ainda"
  description="Crie o primeiro orçamento para começar."
  action={<Button>Criar orçamento</Button>}
/>
```
