/**
 * Módulo de planos temporariamente desativado (em fase de planejamento).
 * Enquanto `PLANS_ENABLED` for `false`, a página `/plano` mostra um aviso de
 * "em fase de planejamento" e o item "Plano" some do menu. Troque para `true`
 * para reativar o módulo por completo (a página real continua no código).
 */
export const PLANS_ENABLED = false;
