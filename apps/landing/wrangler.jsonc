{
  // Landing estática (Next export) servida como Workers Static Assets.
  // Sem `main`: é um site 100% estático, sem código de Worker.
  "name": "floreei-landing",
  "compatibility_date": "2025-06-01",
  // `directory` é relativo a este arquivo → apps/landing/out (saída do next export).
  "assets": { "directory": "./out" }
}
